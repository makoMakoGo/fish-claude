# Code Search

<important if="you need to explore code by behavior, intent, architecture, or unknown implementation location">
- Use `rg` first for code and text search.
- Use `rg --files` when you need to discover files by path or extension.
- Use broader query terms first, then narrow with exact literal matches for confirmation.
</important>
