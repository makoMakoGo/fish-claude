# Code Search

<important if="you need to explore code by behavior, intent, architecture, or unknown implementation location">
- Use `semble_search` first with the current workspace as `repo`.
- Use `semble_find_related` after a promising result.
- Use `grep` only for exact literal matches or exhaustive confirmation.
</important>
